[package]
name = "mesh_network"
version = "0.1.0"
edition = "2021"

[dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }
tokio = { version = "1.0", features = ["full"] }
prost = "0.12"
tonic = "0.10"

[build-dependencies]
tonic-build = "0.10"
